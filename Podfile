platform :ios, '9.0'

target 'IOS-ReactorKit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod "RxSwift"
    pod "RxCocoa"
    pod "ReactorKit"
    pod "SnapKit"

end
